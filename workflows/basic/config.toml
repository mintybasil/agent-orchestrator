poll_interval_secs = 60
assigned_to = "zeroklaw"
allowed_issue_creators = ["mintybasil", "zeroklaw"]

[[repos]]
owner = "zerokrab"
repo = "bento-hancho"

[[repos]]
owner = "zerokrab"
repo = "bento-bench"

# Workflow steps executed sequentially for each assigned issue.
# Template placeholders: {{owner}}, {{repo}}, {{issue_number}}, {{output_path}}, {{step_N_output}}

[[steps]]
name = "Plan"
prompt_template = "Read GitHub issue #{{issue_number}} in {{owner}}/{{repo}}. Use the writing-plans skill, and store the plan in {{output_path}}/plan.md."
profile = "pm"

[[steps.post_hooks]]
type = "file_not_empty"
path = "{{output_path}}/plan.md"

[[steps]]
name = "Implement"
prompt_template = "Use the executing-plans skill to implement {{output_path}}/plan.md. Push your changes to a new branch and open a PR."
profile = "swe"
worktree = true

[[steps.post_hooks]]
type = "file_not_empty"
path = "{{output_path}}/implementation.md"